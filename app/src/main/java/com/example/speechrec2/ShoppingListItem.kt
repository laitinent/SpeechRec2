package com.example.speechrec2

/**
 * @param title - text to display
 * @param collected - true displays as text strike through
 */
data class ShoppingListItem(var title: String, var collected: Boolean = false)